version: "3.8"

services:
  invest-bot:
    build: .
    container_name: invest-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data    # БД сохраняется между перезапусками
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Moscow
